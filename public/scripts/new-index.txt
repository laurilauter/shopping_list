import { IItem } from "../../api/models/itemModel";

//item class
//this class is created for each item on two occasions
//1. from response after successfully inserting an item
//2. from looping through the initial list from database

// variables

// name: string
// active: boolean

// methods

//insert item
//edit item //two steps
//toggle active
//delete item

//redraw current item from response

//-----------------
//general helpers
//
//load inital items list
//
//hide/show completed items

class Item implements IItem {
  name: string;
  active: boolean;

  constructor(name: string, active: boolean) {
    this.name = name;
    this.active = active;
  }

  greet() {
    return "Hello, " + this.name;
  }

  //insert item
  //edit item //two steps
  //toggle active
  //delete item

  //redraw current item from response
}

let something = new Item("Bread", true);

console.log("something", something.greet());
